# ── Existing (data collection) ────────────────────────────────────────────────
requests==2.31.0
beautifulsoup4==4.12.3
pandas==2.2.0
python-dotenv==1.0.1
openai>=1.0.0
lxml==5.1.0
duckduckgo-search>=5.0.0
scrapy>=2.11.0
openpyxl
xlrd
numpy
python-dateutil
pytz
tzdata
setuptools
wheel
six
typing-extensions
jsonschema
certifi
idna
charset-normalizer
urllib3

# ── Geocoding & POI enrichment (free, no API key) ────────────────────────────
geopy>=2.4.0       # Nominatim client (address → lat/lng)
overpy>=0.7.0      # Overpass API client (lat/lng → nearby POIs)

# ── Embeddings (local, free, lightweight) ────────────────────────────────────
# fastembed runs all-MiniLM-L6-v2 via ONNX (~50MB) — same vectors as
# sentence-transformers/torch but small enough for free-tier deploys.
fastembed>=0.3.0

# ── Supabase (vector DB + backend) ───────────────────────────────────────────
supabase>=2.0.0

# ── LLM — Groq (free tier: 14,400 req/day) ───────────────────────────────────
groq>=0.4.0

# ── Web API & chat interface ──────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9   # needed for FastAPI file uploads

# ── Telegram bot (optional — needed only if using Telegram interface) ─────────
python-telegram-bot>=21.0

# ── Google Calendar integration (optional — free API) ────────────────────────
google-api-python-client>=2.100.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0
chardet>=5.0.0,<6.0.0
